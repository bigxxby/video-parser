# Pragmatic Play - Скрытые API Endpoints

## 🎯 Основные находки

Я проанализировал Pragmatic Play реплеи и нашел следующие эндпоинты для работы с демо играми.

---

## 📡 API Endpoints

### 1. **Получение данных реплея** (ГЛАВНЫЙ ЭНДПОИНТ)

**URL:** `https://{dynamic-domain}/ReplayServiceGlobal/api/replay/data`

**Метод:** `GET`

**Параметры:**
- `token` - токен сессии (извлекается из URL реплея)
- `roundID` - ID раунда игры
- `envID` - ID окружения (обычно `100`)

**Пример запроса:**
```
https://www.cnpmciozgh.net/ReplayServiceGlobal/api/replay/data?token=ATuDxqTOJj&roundID=57457474723102&envID=100
```

**Структура ответа:**
```json
{
  "init": "string с параметрами инициализации игры",
  "log": [
    {
      "action": "doSpin",
      "response": {
        // Результаты спина: символы, выигрыши, множители
      }
    },
    {
      "action": "doCollect",
      "response": {
        // Сбор выигрыша
      }
    }
  ]
}
```

**Что содержит `init`:**
- Настройки барабанов
- Таблица выплат
- Лимиты ставок
- Конфигурация игры

**Что содержит `log`:**
- Последовательность всех действий в реплее
- Координаты выпавших символов
- Множители
- Суммы выигрышей
- Триггеры бонусных раундов

---

### 2. **Метаданные игры**

**URL:** `https://{game-cdn}/gs2c/common/v5/games-html5/games/vs/{game_symbol}/desktop/client/game.json`

**Метод:** `GET`

**Описание:** Возвращает правила игры, математику, конфигурацию барабанов

**Пример:**
```
https://[CDN_URL]/gs2c/common/v5/games-html5/games/vs/vs20olympgate/desktop/client/game.json
```

---

### 3. **Ресурсы игры**

**URL:** `https://{game-cdn}/gs2c/common/v5/games-html5/games/vs/{game_symbol}/desktop/client/resources.json`

**Метод:** `GET`

**Описание:** Список всех ресурсов игры (спрайты, звуки, анимации)

---

## 🔑 Как извлечь параметры для API

Все необходимые параметры находятся в исходном коде HTML страницы реплея в вызове `Html5GameManager.init()`:

```javascript
Html5GameManager.init({
  replaySystemUrl: "https://www.cnpmciozgh.net",      // Базовый URL API
  replaySystemContextPath: "/ReplayServiceGlobal",     // Путь сервиса
  replayRoundId: "57457474723102",                     // ID раунда
  environmentId: "100",                                 // ID окружения
  mgckey: "ATuDxqTOJj",                                // Токен (из URL)
  symbol: "vs20olympgate",                              // Символ игры
  gameLoadUrl: "https://[CDN]/path/to/game",           // URL для загрузки игры
  // ... другие параметры
});
```

---

## 🌐 Динамические домены

> ⚠️ **ВАЖНО:** Домены для API могут меняться в зависимости от региона или оператора!

Найденные домены:
- `www.cnpmciozgh.net`
- `euioa.jxcsysekgu.net`
- `www.pplink.social`
- `www.ppshare.net`

**Решение:** Всегда извлекайте `replaySystemUrl` из конфигурации страницы, не хардкодьте домены!

---

## 💡 Использование

### Вариант 1: Через скрипт `pragmatic-api-explorer.js`

```bash
node pragmatic-api-explorer.js
```

Скрипт автоматически:
1. Извлекает конфигурацию из HTML страницы
2. Формирует правильный API URL
3. Получает данные реплея
4. Сохраняет результаты в JSON файлы

### Вариант 2: Прямой API запрос (если знаете параметры)

```javascript
const replayUrl = 'https://www.pplink.social/ATuDxqTOJj';

// 1. Парсим страницу и извлекаем config
const config = await extractGameConfig(replayUrl);

// 2. Формируем API запрос
const apiUrl = `${config.replaySystemUrl}${config.replaySystemContextPath}/api/replay/data`;
const params = `?token=${config.mgckey}&roundID=${config.replayRoundId}&envID=${config.environmentId}`;

// 3. Получаем данные
const response = await fetch(apiUrl + params);
const data = await response.json();

console.log(data);
```

---

## 📊 Структура данных реплея

### Init данные
Содержит строку с параметрами в формате, который понимает игра:
- Конфигурация барабанов
- Символы и их веса
- Таблица выплат
- RTP настройки

### Log данные (массив действий)
Каждое действие содержит:
```json
{
  "action": "doSpin",
  "response": {
    "coins": 1000,      // Сумма выигрыша
    "gameData": {
      "reel": [         // Выпавшие символы
        [9, 0, 10, 5],
        [9, 11, 1, 4],
        // ...
      ],
      "multipliers": {  // Множители (если есть)
        "values": [2, 3, 5, 10]
      }
    }
  }
}
```

---

## 🚀 Возможности

С помощью этих эндпоинтов можно:

✅ Получать полную информацию о реплее без запуска игры  
✅ Анализировать последовательность спинов  
✅ Извлекать множители и выигрыши  
✅ Получать метаданные игр  
✅ Скачивать ресурсы игр  
✅ Автоматизировать обработку реплеев  

---

## 🛠️ Дополнительные эндпоинты

### Телеметрия (логирование)
```
https://clctr.ltguevmavv.com/collect
```
Используется Grafana Faro для мониторинга ошибок. Можно игнорировать.

---

## 📝 Примечания

1. **Аутентификация не требуется** - эндпоинты публичные для демо реплеев
2. **CORS защита** - запросы нужно делать через браузер или puppeteer
3. **Rate limiting** - неизвестен, используйте разумные задержки между запросами
4. **Кэширование** - данные реплеев статичны, можно кэшировать локально

---

## 🎬 Применение для вашего проекта

Вы можете использовать эти эндпоинты для:

1. **Предварительный анализ** - проверить реплей перед записью видео
2. **Фильтрация** - пропускать реплеи без интересных моментов
3. **Метаданные** - получить название игры, множитель без запуска браузера
4. **Оптимизация** - записывать только ключевые моменты из `log`
5. **Дебаг** - понять, что происходит в игре на каждом шаге

---

## 📞 Контакты

Если нужна помощь с интеграцией - обращайтесь!

---

_Исследовано: 2025-12-29_  
_Источник: Анализ сетевых запросов Pragmatic Play реплеев_
